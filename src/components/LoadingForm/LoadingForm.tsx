// Dependencies
import * as React from 'react';

// Internals
import { Container, Text } from './styles';

export const LoadingForm = (): JSX.Element => {
  return (
    <Container>
      <Text>Loading your payment form</Text>
    </Container>
  );
};

export default LoadingForm;
